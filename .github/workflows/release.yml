name: 'release'
on:
  pull_request:
    branches:
      - 'stable'
  workflow_dispatch:

jobs:
  release-os:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, ubuntu-latest, windows-latest]

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v3
      
      - uses: ./.github/actions/deps

      - name: build (${{ matrix.platform }})
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__ # the action automatically replaces \_\_VERSION\_\_ with the app version
          releaseName: 'Horizon v__VERSION__'
          releaseBody: 'See the assets to download this version and install.'
          releaseDraft: true
          prerelease: false

  build-android:
    strategy:
      matrix:
        target: ["aarch64", "armv7", "i686", "x86_64"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: ./.github/actions/deps
        id: 'deps'
        with:
          android: '1'

      - name: build (android)
        run: yarn tauri android build -t ${{ matrix.target }}
        env:
          NDK_HOME: ${{ steps.deps.outputs.ndk-path }}

      - name: upload (android)
        uses: actions/upload-artifact@v3
        with:
          name: android
          path: |
            src-tauri/gen/android/app/build/outputs/apk/**/**/*.apk

  # build-ios:
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v3

  #     - uses: ./.github/actions/deps
  #       with:
  #         ios: '1'

  #     - name: build (ios)
  #       run: yarn tauri ios build
  #       env:
  #         TAURI_APPLE_DEVELOPMENT_TEAM: "PIPPAGAY"
